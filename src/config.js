const config = {
    clipboard_content_check_interval: 500,
    time_format: 'YYYY-MM-DD HH:mm:ss',
    simple_content_length: 20,
    shortcut_key: {
        win: {
            quit: "Ctrl+Shift+Q",
            show: "Ctrl+Shift+1"
        },
        mac: {
            quit: "Control+Shift+Q",
            show: "Control+Shift+1"
        }
    }
}

export default config